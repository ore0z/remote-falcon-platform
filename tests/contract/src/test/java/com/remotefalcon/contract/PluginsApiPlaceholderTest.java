package com.remotefalcon.contract;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertTrue;

/**
 * Placeholder test for tests/contract module — Sprint 1 PR A scaffolding only.
 * Replaced in Sprint 3 with PluginsApiContractTest that loads captured plugin
 * payload fixtures from tests/fixtures/plugin-requests/ and replays them
 * against a testcontainers plugins-api stack.
 *
 * Until then, this exists so the test-contract CI job has something to run.
 */
class PluginsApiPlaceholderTest {

    @Test
    void placeholder() {
        // Sprint 3: replace with real contract assertions.
        assertTrue(true);
    }
}
