# Environment files (contain secrets when populated)
.env
.env.*
!.env.*.example
!.env.example

# Ad-hoc credential / scratch notes (e.g., posthog.md with API key)
posthog.md
secrets.md
*.creds
*.secrets

# Build outputs
**/target/
**/build/
**/.gradle/
**/node_modules/
**/dist/

# Test / coverage outputs
**/coverage/
**/.nyc_output/
**/jacoco/
**/test-output/

# IDE / editor
.idea/
.vscode/
*.iml
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Operator baselines (sensitive cluster snapshots)
baseline-*/

# docs-screenshots tier output (Playwright-generated PNGs + manifest).
# Synced into remote-falcon-docusaurus via that repo's docs:sync script.
docs-output/

# Claude Code agent worktrees (created automatically when running
# Agent({isolation: "worktree"}) — local-only, never committed).
.claude/
