import LogoMark from '../design-system/components/LogoMark';

// Legacy wrapper kept for any callers still importing this path.
// Renders the canonical design-system LogoMark (public/rf-icon.png).
const Logo = () => <LogoMark size={70} />;
export default Logo;
