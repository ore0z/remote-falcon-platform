// KeyFeature has been consolidated into Feature.jsx as part of the
// marketing refresh — the four feature blocks now flow through one
// continuous section. This stub is kept so any stragglers importing
// the file render harmlessly; remove during legacy cleanup.

const KeyFeature = () => null;

export default KeyFeature;
