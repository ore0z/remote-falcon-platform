// theme constant
export const gridSpacing = 3;

// Sidebar widths.
//   `drawerWidth`           — full-width sidebar in expanded mode (and the
//                             legacy fixed value, kept for any code that
//                             hasn't migrated to the rail pattern yet).
//   `drawerWidthExpanded`   — same value, exported under a clearer name.
//   `drawerWidthCollapsed`  — icon rail width when the sidebar is collapsed.
//   `topbarHeight`          — slim v2 topbar height (Phase 3 mockup).
//
// Per the v2 dashboard mockup at apps/ui/docs/design-system/mockup.html.
export const drawerWidth = 248;
export const drawerWidthExpanded = 248;
export const drawerWidthCollapsed = 72;
export const appDrawerWidth = 320;
export const topbarHeight = 56;

export const unexpectedErrorMessage = 'Unexpected Error';
