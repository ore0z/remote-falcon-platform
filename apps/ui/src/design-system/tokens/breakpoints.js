/**
 * Breakpoints — kept compatible with MUI's defaults so existing
 * `theme.breakpoints.*` calls continue to work during migration.
 */
export const breakpoints = {
  xs: 0,
  sm: 600,
  md: 900,
  lg: 1200,
  xl: 1536
};

export default breakpoints;
