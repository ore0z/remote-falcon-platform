# Testcontainers configuration for CI/CD environments
# This file ensures Testcontainers works properly in GitHub Actions

# Disable Ryuk container checks (optional, can speed up CI)
# ryuk.container.privileged=true

# Enable reuse of containers for faster test execution
testcontainers.reuse.enable=false

# Docker host configuration (uses default Docker socket)
# docker.client.strategy=org.testcontainers.dockerclient.UnixSocketClientProviderStrategy
